import { SubcategoriesClient } from "./subcategories-client";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Sub category | Admin",
};

export default function SubcategoriesPage() {
  return <SubcategoriesClient />;
}
