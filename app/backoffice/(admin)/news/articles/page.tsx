import { PlaceholderPanel } from "../../_components/placeholder-panel";

export const metadata = { title: "All stories | Admin" };

export default function NewsArticlesPage() {
  return (
    <PlaceholderPanel
      title="All stories"
      description="Full list of published, draft, and archived stories — filters and search coming later."
    />
  );
}
