import { PlaceholderPanel } from "../../_components/placeholder-panel";

export const metadata = { title: "Drafts | Admin" };

export default function NewsDraftsPage() {
  return (
    <PlaceholderPanel
      title="Drafts"
      description="Stories not yet published — assign editors and track deadlines here."
    />
  );
}
