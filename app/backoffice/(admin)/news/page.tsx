import { PlaceholderPanel } from "../_components/placeholder-panel";

export const metadata = { title: "News desk | Admin" };

export default function NewsDeskPage() {
  return (
    <PlaceholderPanel
      title="News desk"
      description="Overview of all stories — trending, pending review, and today’s pipeline."
    />
  );
}
