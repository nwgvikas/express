import { PlaceholderPanel } from "../../_components/placeholder-panel";

export const metadata = { title: "Published | Admin" };

export default function NewsPublishedPage() {
  return (
    <PlaceholderPanel
      title="Published stories"
      description="Stories visible on the live site — manage URL, views, and last edit here."
    />
  );
}
