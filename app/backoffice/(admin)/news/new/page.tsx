import { PlaceholderPanel } from "../../_components/placeholder-panel";

export const metadata = { title: "New story | Admin" };

export default function NewsNewPage() {
  return (
    <PlaceholderPanel
      title="New story"
      description="Editor for a new article or story will open here — headline, body, featured image, tags."
    />
  );
}
