import { PlaceholderPanel } from "../../_components/placeholder-panel";

export const metadata = { title: "Schedule | Admin" };

export default function NewsScheduledPage() {
  return (
    <PlaceholderPanel
      title="Schedule / timed publishing"
      description="Stories scheduled for a future date and time — a calendar view can be added later."
    />
  );
}
