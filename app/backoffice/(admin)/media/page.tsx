import { PlaceholderPanel } from "../_components/placeholder-panel";

export const metadata = { title: "Media library | Admin" };

export default function MediaPage() {
  return (
    <PlaceholderPanel
      title="Media library"
      description="Photos, videos, and files — grid, folders, and CDN links from here."
    />
  );
}
