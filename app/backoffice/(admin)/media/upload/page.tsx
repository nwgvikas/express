import { PlaceholderPanel } from "../../_components/placeholder-panel";

export const metadata = { title: "Upload | Admin" };

export default function MediaUploadPage() {
  return (
    <PlaceholderPanel
      title="Media upload"
      description="Drag-and-drop upload, alt text, and credit line — batch upload coming later."
    />
  );
}
