import { PlaceholderPanel } from "../_components/placeholder-panel";

export const metadata = { title: "Sections | Admin" };

export default function SectionsPage() {
  return (
    <PlaceholderPanel
      title="Sections / Categories"
      description="Sections like Breaking, Local, Sports, Opinion — slug, order, and menu placement."
    />
  );
}
