import { PlaceholderPanel } from "../../_components/placeholder-panel";

export const metadata = { title: "New section | Admin" };

export default function SectionsNewPage() {
  return (
    <PlaceholderPanel
      title="New section"
      description="Form to add a category or vertical — name, slug, and parent section."
    />
  );
}
